---

const { date } = Astro.props;
const datetime = new Date(date);
---

<time>
	{datetime.toLocaleDateString('en-us', {
	  weekday: 'long',
	  year: 'numeric',
	  month: 'long',
	  day: 'numeric',
	})}
  </time>

